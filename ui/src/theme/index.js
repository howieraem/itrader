import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      lighter: '#0077b7',
      light: '#006da7',
      main: '#005480',
      dark: '#003e5e',
      darker: '#002d45',
      contrastText: '#ffffff',
    },
  },
})

export default theme;
